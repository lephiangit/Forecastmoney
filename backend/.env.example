SUPABASE_URL=
SUPABASE_KEY=
GEMINI_API_KEY=

# ⚠️ MUST change these in production!
# Generate a secure key: python -c "import secrets; print(secrets.token_urlsafe(32))"
ADMIN_PASSWORD=admin123
ADMIN_SECRET_KEY=change-this-in-production

# CORS: comma-separated allowed origins (use * for dev, restrict in production)
# Example: ALLOWED_ORIGINS=https://your-app.netlify.app,http://localhost:3000
ALLOWED_ORIGINS=*
